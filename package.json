{
  "name": "react-template",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "bcrypt": "^3.0.4",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.17.2",
    "connect-mongo": "^2.0.3",
    "d3-shape": "^1.3.7",
    "dotenv": "^6.2.0",
    "express": "^4.15.3",
    "express-session": "^1.15.4",
    "mongoose": "^5.4.15",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0",
    "react": "^16.8.3",
    "react-dom": "^16.8.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "react-scripts": "2.1.5",
    "nodemon": "^2.0.2",
    "morgan": "^1.8.2"
  },
  "scripts": {
    "start": "node server.js",
    "eject": "react-scripts eject",
    "dev": "concurrently \"react-scripts start\" \"nodemon server.js\"",
    "prod": "cd client && yarn install && cd client && yarn run build && export NODE_ENV=\"production\" && node server.js",
    "heroku-postbuild": "yarn install && cd client && npm install && npm run build"
  },
  "engines": {
    "node": "12.12.0"
  },
  "proxy": "http://localhost:8080",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
